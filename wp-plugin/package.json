{
  "name": "{{ package.name }}",
  "description": "{{ description }}",
  "license": "{{ license }}",
  "private": true,
  "author": {
    "name": "{{ author.name }}",
    "email": "{{ author.email }}"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
