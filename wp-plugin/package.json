{
  "name": "{{ package.name }}",
  "description": "{{ description }}",
  "license": "{{ license.slug }}",
  "private": true,
  "author": {
    "name": "{{ author.name }}",
    "email": "{{ author.email }}"
  }
}
